# Assumptions and Unknowns

> **Public Example Report — Sanitized**
> This is a sanitized demonstration report generated by [CloudoX](https://cloudox.io).
> AWS account IDs, resource identifiers (VPC, subnet, security-group, IAM role, etc.),
> organisation IDs, and any customer- or employer-identifying names have been replaced
> with realistic synthetic values using a deterministic mapping.
> Architecture patterns, relationships, and findings are preserved for demonstration
> purposes. This report does **not** represent any real AWS environment or customer.

---

## Assumptions Made in This Report

| # | Assumption | Basis | Confidence | How to Validate |
|---|-----------|-------|------------|-----------------|
| A1 | The account `118535197059` represents a sandbox environment | Explicit `classification: sandbox` tag/classification in discovery data with `explicit: true` | High | Confirm with account owner that no production workloads share this account |
| A2 | The two inferred workloads (`dx-discovery-test-alb`, `dx-discovery-test-discovery`) are test or discovery-tooling workloads, not production services | Naming convention (`dx-discovery-test-*`); workload confidence rated "medium" and "low" respectively | Low | Customer to confirm purpose and owner of both workloads |
| A3 | The `dx-discovery-test-alb` load balancer is the primary ingress point for the ALB-anchored workload | Finding `exposure:AWS::ElasticLoadBalancingV2::LoadBalancer:dx-discovery-test-alb` and workload anchor field | Medium | Confirm whether any other ingress paths (CloudFront, API Gateway, direct EC2) exist |
| A4 | The two EC2 instances (`AWS::EC2::Instance` × 2) are targets behind the ALB | Target group `dx-discovery-test-tg` is in the same workload resource list; no explicit instance-to-target-group relationship was detected in the graph | Low | Validate via target group registration in the AWS console or CLI |
| A5 | The single CloudTrail trail resource represents the account's audit logging mechanism | Resource type `AWS::CloudTrail::Trail` × 1 present in graph | Low | Trail status (enabled/disabled, single- vs. multi-region, S3 destination) was not captured; must be confirmed |
| A6 | The single GuardDuty detector resource represents a detector in `eu-central-1` | Resource type `AWS::GuardDuty::Detector` × 1; all workloads are in `eu-central-1` | Low | Detector enablement status and findings were not present in the discovery data; must be confirmed |
| A7 | The five VPC endpoints (`AWS::EC2::VPCEndpoint` × 5) provide private connectivity for services used by the workloads | Co-location in the same account/region; no explicit relationship to workload resources was detected | Low | Identify endpoint service names and which resources use them |

---

## Unknowns and Gaps

### Ownership and Environment Structure
*(affects `03-environment-structure.md`)*

- **No AWS Organization detected.** `organization_count: 0` in graph stats. It is unknown whether this account is standalone or a member of an unscanned organization. Multi-account governance controls (SCPs, delegated admin) cannot be assessed.
- **No environment tags observed on resources.** Environment classification was derived from account-level metadata, not resource tags. Tag coverage and consistency across the 277 resources is unknown.
- **No team or owner tags detected.** Resource ownership cannot be attributed to any team or individual from the available data.

### Architecture
*(affects `02-architecture-overview.md`)*

- **EC2 instance configuration is unknown.** Two instances are present but their AMI, OS, instance type, patch state, and role are not in the discovery data.
- **Lambda function details are sparse.** `dx-discovery-test-discovery` is present but its runtime, handler, trigger sources, and IAM execution role bindings are not captured. Workload confidence is rated "low".
- **No RDS, DynamoDB, ECS, EKS, or other compute/data-tier resources detected.** It is unknown whether these services are absent or simply not collected by the discovery run.
- **67 IAM roles and 25 inline policies are present** but their trust relationships, attached policies, and effective permissions were not surfaced in this section. The blast radius of any over-privileged role is unknown.

### Networking
*(affects `04-networking.md`)*

- **Two VPCs are present** (`AWS::EC2::VPC` × 2) but their CIDR ranges, peering relationships, and purpose are not in the filtered context. It is unknown whether they are isolated or connected.
- **Route table and NACL configurations** (8 route tables, 3 NACLs) were not fully surfaced. Private vs. public subnet segmentation beyond the six IGW-routed subnets is unknown.
- **Route53 Resolver rule** (`AWS::Route53Resolver::ResolverRule` × 1) and two associations are present. The on-premises or shared-services DNS target is unknown.

### Security
*(affects `05-security-overview.md`)*

- **CloudTrail trail status unknown.** The trail resource exists (`AWS::CloudTrail::Trail` × 1) but whether it is enabled, its S3 destination, log file validation, and region scope are not in the discovery data.
- **GuardDuty detector status unknown.** The detector resource exists (`AWS::GuardDuty::Detector` × 1) but its enablement state, finding statistics, and member account configuration are not captured.
- **No Security Hub, Config, or Access Analyzer resources detected.** It is unknown whether these services are absent or were not collected.
- **5 KMS keys and 16 aliases present** but key policies, rotation status, and which resources they protect are unknown.
- **7 S3 buckets present** but bucket policies, public access block settings, encryption configuration, and versioning state are not in the discovery data.
- **1 SecretsManager secret present** but its rotation configuration and which resources consume it are unknown.
- **No explicit risk records were generated** (`risk_count: 0`). Seven findings exist (all exposure category); no severity ratings were assigned in the discovery data.

### Observability
*(affects `06-observability.md`)*

- **Only 1 CloudWatch alarm and 4 log groups detected.** Whether these cover the workloads or are incidental is unknown. No dashboards, metric filters, or SNS topics were detected.
- **No X-Ray, CloudWatch Synthetics, or third-party APM resources detected.** Absence may reflect scope of the collector rather than actual absence.
- **Log group retention, subscription filters, and destinations** are not in the discovery data.

---

## Data Sources That Would Close Real Gaps

| Gap | Data Source Needed |
|-----|--------------------|
| CloudTrail trail enablement, scope, and S3 destination | `aws cloudtrail get-trail-status` + `describe-trails` |
| GuardDuty detector status and active findings | `aws guardduty get-detector` + `list-findings` |
| S3 bucket policies, public access block, encryption | `aws s3api get-bucket-policy`, `get-public-access-block`, `get-bucket-encryption` |
| IAM role trust relationships and effective permissions | IAM Access Analyzer findings; `aws iam get-role`, `simulate-principal-policy` |
| EC2 instance details (type, AMI, patch state) | `aws ec2 describe-instances` with full attribute capture |
| KMS key rotation and key policy | `aws kms get-key-rotation-status`, `get-key-policy` |
| VPC CIDR ranges and peering connections | `aws ec2 describe-vpcs`, `describe-vpc-peering-connections` |
| VPC endpoint service names and associations | `aws ec2 describe-vpc-endpoints` |
| Organization structure and SCPs | `aws organizations describe-organization`, `list-policies` |
| CloudWatch alarms, log groups, and metric filters | Full CloudWatch collector run with log group details |

---

## Workshop Questions

### Ownership and Environment
1. Is account `118535197059` a standalone account or part of an AWS Organization? If part of an org, which management account governs it, and are SCPs applied?
2. Who owns the `dx-discovery-test-*` resources — are these a permanent test environment or ephemeral tooling? Is there a separate production account?

### Architecture
3. What is the intended purpose of the two EC2 instances — are they the application tier behind `dx-discovery-test-tg`, and what workload do they run?
4. What does the Lambda function `dx-discovery-test-discovery` do, and what triggers it? Is it part of the discovery tooling or an application component?
5. Are there data-tier services (RDS, DynamoDB, ElastiCache) in use that were not captured in this discovery run?

### Security
6. Is the CloudTrail trail currently enabled, and does it cover all regions? Where are logs delivered, and is log file validation active?
7. Is the GuardDuty detector active? Are findings reviewed, and is there a response process?
8. What is the purpose of the single SecretsManager secret — which application or service consumes it, and is rotation enabled?
9. The security group `sg-0ad00d699b43544b3` (`dx-discovery-test-sg-alb`) allows ingress from `0.0.0.0/0`. Is this intentional for the sandbox, and is the same pattern present in any production account?

### Networking
10. What are the two VPCs used for — are they isolated environments, or is there peering or Transit Gateway connectivity between them?
11. What is the Route53 Resolver rule resolving — does it indicate connectivity to on-premises DNS or a shared-services VPC?

### Observability and Operations
12. The single CloudWatch alarm and four log groups appear sparse for the detected workloads. What monitoring and alerting is in place, and is there a third-party observability tool (Datadog, Splunk, etc.) not visible in this discovery?
13. Is there a defined incident response or on-call process for this account, and if so, where are alerts routed?