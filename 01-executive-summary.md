> **Public Example Report — Sanitized**
> This is a sanitized demonstration report generated by [CloudoX](https://cloudox.io).
> AWS account IDs, resource identifiers (VPC, subnet, security-group, IAM role, etc.),
> organisation IDs, and any customer- or employer-identifying names have been replaced
> with realistic synthetic values using a deterministic mapping.
> Architecture patterns, relationships, and findings are preserved for demonstration
> purposes. This report does **not** represent any real AWS environment or customer.

---

## Key Takeaways

- **Single sandbox account** (`118535197059`) with 277 resources across a modest footprint; the environment is explicitly classified as `sandbox` with high confidence.
- **Internet exposure is present**: an internet-facing Application Load Balancer (`dx-discovery-test-alb`), five public subnets, and a security group (`sg-0ad00d699b43544b3`) with `0.0.0.0/0` ingress rules are flagged across 7 high-confidence exposure findings.
- **Two inferred workloads** were detected — one anchored on the ALB (`dx-discovery-test-alb`, medium confidence) and one on a Lambda function (`dx-discovery-test-discovery`, low confidence) — both in `eu-central-1`.
- **No explicit risk records were generated** (`risk_count: 0`), but 7 exposure findings exist and warrant review before any promotion beyond sandbox.
- **Monitoring and IAM depth are unclear**: a GuardDuty detector and a CloudTrail trail resource are present, but their operational status cannot be confirmed from the available data.

---

## Scope at a Glance

| Dimension | Value | Source |
|---|---|---|
| Accounts | 1 (`118535197059`) | `graph_stats.account_count` |
| Environments | 1 (sandbox, explicit, high confidence) | `environments` block |
| Regions with resources | Not detected in the provided discovery data | `graph_stats.region_count: 0` |
| Total resources | 277 | `graph_stats.resource_count_total` |
| Relationships mapped | 107 | `graph_stats.relationship_count_total` |
| Exposure findings | 7 (all high confidence) | `graph_stats.finding_count` |
| Explicit risk records | 0 | `graph_stats.risk_count` |
| Inferred workloads | 2 | `graph_stats.workload_count` |
| Inferred systems | 0 | `graph_stats.system_count` |

> **Note:** `graph_stats.region_count` is reported as `0`. Resource-level data places both inferred workloads in `eu-central-1`; the region count should be validated with the customer.

---

## Inferred Workloads

| Workload name | Anchor resource | Confidence | Internet-facing | Region | Basis |
|---|---|---|---|---|---|
| `dx-discovery-test-alb` | `dx-discovery-test-alb` (`AWS::ElasticLoadBalancingV2::LoadBalancer`) | Medium | No (workload flag); **but** an exposure finding marks the ALB as internet-facing — validate | `eu-central-1` | Workload block + finding `exposure:AWS::ElasticLoadBalancingV2::LoadBalancer:dx-discovery-test-alb` |
| `dx-discovery-test-discovery` | `dx-discovery-test-discovery` (`AWS::Lambda::Function`) | Low | No | `eu-central-1` | Workload block |

The `internet_facing` flag on the ALB workload is `false` in the workload block, yet the exposure finding explicitly states the load balancer scheme is internet-facing. This inconsistency should be validated with the customer.

---

## Internet Exposure Summary

Seven high-confidence exposure findings were generated, all in the `exposure` category:

| Finding | Impacted resource | Description |
|---|---|---|
| ALB internet-facing | `dx-discovery-test-alb` | Load balancer scheme is internet-facing |
| Public subnet | `subnet-013918fce0a052014` | Route table forwards to an Internet Gateway |
| Public subnet | `subnet-02cfd56481b2491e5` | Route table forwards to an Internet Gateway |
| Public subnet | `subnet-0a0bd62eb89349282` | Route table forwards to an Internet Gateway |
| Public subnet | `subnet-07f04a1f0266b49d2` | Route table forwards to an Internet Gateway |
| Public subnet | `subnet-0d65afe24e85aacbe` | Route table forwards to an Internet Gateway |
| Open security group | `sg-0ad00d699b43544b3` (`dx-discovery-test-sg-alb`) | Ingress allows `0.0.0.0/0` or `::/0` |

See [`07-findings-and-risks.md`](07-findings-and-risks.md) for detailed treatment and [`04-networking.md`](04-networking.md) for network context.

---

## Notable Resource Signals

The following resource types appear in the inventory and carry implications worth validating:

- **IAM**: 67 roles, 25 inline role policies, 61 role-policy attachments, and 5 managed policies. The IAM footprint appears large relative to the overall resource count; whether this reflects automation scaffolding or active use is unknown.
- **KMS**: 5 keys and 16 aliases present. Encryption scope and key ownership are not determinable from the available data.
- **CloudFormation**: 3 stacks detected, suggesting at least some infrastructure is managed as code. Stack names and outputs are not available in this context.
- **GuardDuty**: 1 detector resource present (`AWS::GuardDuty::Detector`). Operational status (enabled/disabled) is not confirmed by the available data.
- **CloudTrail**: 1 trail resource present (`AWS::CloudTrail::Trail`). Whether it is active and what it covers is not confirmed by the available data.
- **S3**: 7 buckets present. Bucket policies, public access block settings, and purpose are not available in this context.
- **Secrets Manager**: 1 secret present. Rotation status and ownership are unknown.

See [`05-security-overview.md`](05-security-overview.md) and [`06-observability.md`](06-observability.md) for deeper treatment.

---

## Major Unknowns

- **Active region(s)**: `graph_stats.region_count` is `0`; workload data suggests `eu-central-1` but this requires confirmation.
- **GuardDuty and CloudTrail status**: Resources exist but operational state is unconfirmed.
- **Ownership and tagging**: No tag-based ownership signals were surfaced in the available data. Whether resources carry environment, team, or cost-allocation tags is unknown.
- **Sandbox promotion intent**: It is unknown whether any workloads in this sandbox account are intended to move to production or are purely experimental.
- **IAM role purpose**: 67 roles is a notable count; whether these are service-linked, automation-generated, or human-assigned is not determinable from the available data.

See [`08-assumptions-and-unknowns.md`](08-assumptions-and-unknowns.md) for the full uncertainty register.

---

## Recommended Discovery Questions

1. **Internet exposure intent**: Is `dx-discovery-test-alb` intentionally internet-facing in this sandbox, and what controls govern access to it and the associated security group `sg-0ad00d699b43544b3`?
2. **Region scope**: Are all workloads confined to `eu-central-1`, or are there resources in other regions not captured in this discovery run?
3. **GuardDuty and CloudTrail**: Are the detected detector and trail actively enabled and delivering findings/logs? Where are logs being delivered?
4. **CloudFormation stacks**: What do the 3 detected stacks deploy, and are they the authoritative source of truth for this environment's infrastructure?
5. **IAM role inventory**: What is the origin of the 67 IAM roles — are they primarily service-linked, CDK/CloudFormation-generated, or manually created?
6. **Sandbox lifecycle**: Is this account a long-lived sandbox or a short-lived test environment? Are there plans to promote any workloads to a production account?
7. **Tagging strategy**: Is there an existing tagging policy, and if so, why are ownership/environment tags not visible in the discovery data?