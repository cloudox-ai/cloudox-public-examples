# Networking — Account 118535197059 (eu-central-1)

> **Public Example Report — Sanitized**
> This is a sanitized demonstration report generated by [CloudoX](https://cloudox.io).
> AWS account IDs, resource identifiers (VPC, subnet, security-group, IAM role, etc.),
> organisation IDs, and any customer- or employer-identifying names have been replaced
> with realistic synthetic values using a deterministic mapping.
> Architecture patterns, relationships, and findings are preserved for demonstration
> purposes. This report does **not** represent any real AWS environment or customer.

---

Two VPCs exist in `eu-central-1`, both with Internet Gateways attached and no NAT gateways, Transit Gateway attachments, or VPC peering connections detected.

## VPC Summary

| VPC ID | Name | CIDR | Environment Tag | Subnets | IGW | NAT GW | Peering | TGW |
|---|---|---|---|---|---|---|---|---|
| `vpc-06e0352782cb28256` | dx-discovery-test-vpc | 10.20.0.0/16 | sandbox | 4 | Yes | No | No | No |
| `vpc-0d7e76c09908626c2` | test-vpc | 10.0.0.0/16 | _(not tagged)_ | 6 | Yes | No | No | No |

**Evidence:** `digest.vpcs`; environment tag on `vpc-06e0352782cb28256` is explicit (`sandbox`). No environment tag was detected on `vpc-0d7e76c09908626c2`.

---

## Subnet Classification

Public subnets are identified by route table entries that forward to an Internet Gateway (digest `public_subnets`). Subnets not listed there are not confirmed public; their classification should be validated with the customer.

### vpc-06e0352782cb28256 (dx-discovery-test-vpc) — 4 subnets

| Subnet ID | Name | AZ | Classification | Evidence |
|---|---|---|---|---|
| `subnet-013918fce0a052014` | dx-discovery-test-public-b | eu-central-1b | **Public** | Routes to `igw-0fc7848b4f015a3cc` via `rtb-05db0868b2cb9ffd4` |
| `subnet-07f04a1f0266b49d2` | dx-discovery-test-public-a | eu-central-1a | **Public** | Routes to `igw-0fc7848b4f015a3cc` via `rtb-05db0868b2cb9ffd4` |
| `subnet-0980c559fb5dcb640` | _(name not in data)_ | _(unknown)_ | Not confirmed public | Routes to `vpce-0e58252535f32f873` (S3 Gateway) via `rtb-0550412284ed21dab`; no IGW route detected |
| `subnet-06300b25ee5d11e54` | _(name not in data)_ | _(unknown)_ | Not confirmed public | Same route table as above (`rtb-0550412284ed21dab`) |

### vpc-0d7e76c09908626c2 (test-vpc) — 6 subnets

| Subnet ID | Name | AZ | Classification | Evidence |
|---|---|---|---|---|
| `subnet-0a0bd62eb89349282` | test-subnet-public2-eu-central-1b | eu-central-1b | **Public** | Routes to `igw-09fc956d52f614cb0` via `rtb-0b8e0917e18420ccf` |
| `subnet-0d65afe24e85aacbe` | test-subnet-public1-eu-central-1a | eu-central-1a | **Public** | Routes to `igw-09fc956d52f614cb0` via `rtb-0b8e0917e18420ccf` |
| `subnet-02cfd56481b2491e5` | test-subnet-public3-eu-central-1c | eu-central-1c | **Public** | Routes to `igw-09fc956d52f614cb0` via `rtb-0b8e0917e18420ccf` |
| `subnet-0be5d8818bcffe1c6` | _(name not in data)_ | _(unknown)_ | Not confirmed public | Routes to `vpce-050c321468753e823` (S3 Gateway) via `rtb-0058fbf3c6b8f8d4f`; no IGW route detected |
| `subnet-0da2ee949655c0819` | _(name not in data)_ | _(unknown)_ | Not confirmed public | Routes to `vpce-050c321468753e823` via `rtb-0d47dbf74603a4d27`; no IGW route detected |
| `subnet-0f56715e273de2bf9` | _(name not in data)_ | _(unknown)_ | Not confirmed public | Routes to `vpce-050c321468753e823` via `rtb-092aebf7bd0a767c3`; no IGW route detected |

---

## Internet-Facing Paths

Three confirmed internet-facing paths are present.

### 1. Internet Gateway — vpc-06e0352782cb28256

`igw-0fc7848b4f015a3cc` is attached to `vpc-06e0352782cb28256`. Route table `rtb-05db0868b2cb9ffd4` forwards to this IGW and is associated with `subnet-013918fce0a052014` and `subnet-07f04a1f0266b49d2`.

### 2. Internet Gateway — vpc-0d7e76c09908626c2

`igw-09fc956d52f614cb0` is attached to `vpc-0d7e76c09908626c2`. Route table `rtb-0b8e0917e18420ccf` forwards to this IGW and is associated with `subnet-0a0bd62eb89349282`, `subnet-0d65afe24e85aacbe`, and `subnet-02cfd56481b2491e5`.

### 3. Internet-Facing Application Load Balancer

`dx-discovery-test-alb` (scheme: `internet-facing`, type: `application`) is deployed in `vpc-06e0352782cb28256`, spanning public subnets `subnet-07f04a1f0266b49d2` and `subnet-013918fce0a052014`. It is secured by security group `sg-0ad00d699b43544b3` (`dx-discovery-test-sg-alb`).

**Security group exposure on the ALB path:**

| Security Group | Name | World-Open Ports | Sensitive World Ports | Evidence |
|---|---|---|---|---|
| `sg-0ad00d699b43544b3` | dx-discovery-test-sg-alb | 80 | None detected | `digest.security_group_exposure`; finding `exposure:AWS::EC2::SecurityGroup:sg-0ad00d699b43544b3` |

Port 443 is not listed as world-open in the digest. Whether HTTPS is handled via a redirect rule or listener on the ALB should be validated.

One Elastic IP (`eipalloc-0d1376aefaed86d40`) is attached to ENI `eni-0ca3748ec1f2a155b`, which is in `subnet-013918fce0a052014` and secured by `sg-0ad00d699b43544b3`. The resource type attached to this ENI is not identified in the provided data.

---

## Private Connectivity — VPC Endpoints

Five VPC endpoints are present. No NAT gateways were detected in either VPC, so subnets without an IGW route appear to rely on these endpoints for AWS service access.

| Endpoint ID | Name | Type | Service | VPC | Subnets |
|---|---|---|---|---|---|
| `vpce-050c321468753e823` | test-vpce-s3 | Gateway | S3 | `vpc-0d7e76c09908626c2` | Routes via `rtb-0058fbf3c6b8f8d4f`, `rtb-0d47dbf74603a4d27`, `rtb-092aebf7bd0a767c3` |
| `vpce-0e58252535f32f873` | dx-discovery-test-vpce-s3 | Gateway | S3 | `vpc-06e0352782cb28256` | Routes via `rtb-0550412284ed21dab` (subnets `subnet-0980c559fb5dcb640`, `subnet-06300b25ee5d11e54`) |
| `vpce-0cfcb91c9f2e17933` | dx-discovery-test-vpce-ssm | Interface | SSM | `vpc-06e0352782cb28256` | `subnet-0980c559fb5dcb640`, `subnet-06300b25ee5d11e54` |
| `vpce-081c9372da8bc7db0` | dx-discovery-test-vpce-secretsmanager | Interface | Secrets Manager | `vpc-06e0352782cb28256` | `subnet-0980c559fb5dcb640`, `subnet-06300b25ee5d11e54` |
| `vpce-06409a69ca69a7a86` | dx-discovery-test-vpce-logs | Interface | CloudWatch Logs | `vpc-06e0352782cb28256` | `subnet-0980c559fb5dcb640`, `subnet-06300b25ee5d11e54` |

The three Interface endpoints (SSM, Secrets Manager, Logs) are all placed in the same two subnets within `vpc-06e0352782cb28256`. This suggests those subnets host resources that require private AWS API access without traversing the internet, though no NAT path exists to confirm or deny internet egress for those subnets independently.

No VPC endpoints were detected for `vpc-0d7e76c09908626c2` beyond the S3 Gateway endpoint.

---

## Network ACLs

One non-default NACL was detected.

| NACL ID | Name | VPC | Entry Count | Explicit Deny Rules | Default |
|---|---|---|---|---|---|
| `acl-0c70bab77515a89f5` | dx-discovery-test-nacl | `vpc-06e0352782cb28256` | 6 | 0 | No |

**Evidence:** `digest.nacl_notes`. The absence of explicit deny rules in this NACL is noted; whether the rules are restrictive or permissive cannot be determined from entry count alone. The specific rules should be reviewed.

Two additional NACLs exist in the graph (`resource_count_by_type` shows 3 total) but their details are not present in the provided discovery data.

---

## Security Groups — Observed Associations

Six security groups are present across both VPCs. Only `sg-0ad00d699b43544b3` has confirmed world-open ingress in the digest.

| Security Group | VPC | Notable Relationships |
|---|---|---|
| `sg-0ad00d699b43544b3` | `vpc-06e0352782cb28256` | Secures `dx-discovery-test-alb`; port 80 open to `0.0.0.0/0` |
| `sg-0929c485a88a87f87` | `vpc-06e0352782cb28256` | Has `allows_from` relationship to `sg-0ad00d699b43544b3` — appears to accept traffic sourced from the ALB security group |
| `sg-054012adde14f592e` | `vpc-06e0352782cb28256` | Secures multiple ENIs in `subnet-0980c559fb5dcb640` and `subnet-06300b25ee5d11e54` |
| `sg-05dcc17603d8eee7a` | `vpc-06e0352782cb28256` | No relationships to resources detected in provided data |
| `sg-0bbc1f0c3f75e4086` | `vpc-0d7e76c09908626c2` | Secures ENIs in `subnet-0d65afe24e85aacbe` |
| `sg-05a6a6b5e78bd9da4` | `vpc-0d7e76c09908626c2` | No relationships to resources detected in provided data |

The `allows_from` relationship between `sg-0929c485a88a87f87` and `sg-0ad00d699b43544b3` suggests a security group chaining pattern (ALB SG → backend SG), but the specific ingress rule details and the resources secured by `sg-0929c485a88a87f87` are not present in the provided data.

---

## Cross-Account and Cross-Region Connectivity

- **VPC Peering:** No peering connections detected (`digest.totals.vpc_peering_connections: 0`).
- **Transit Gateway:** No Transit Gateways or attachments detected (`digest.totals.transit_gateways: 0`, `transit_gateway_attachments: 0`).
- **VPN:** No VPN connections detected (`digest.totals.vpn_connections: 0`; confirmed on both VPCs).
- **Cross-region:** All resources are in `eu-central-1`. No cross-region networking was detected.

The two VPCs have no detected connectivity between them. Whether this is intentional or whether connectivity is expected should be confirmed with the customer.

---

## Route 53 Resolver

One `AWS::Route53Resolver::ResolverRule` and two `AWS::Route53Resolver::ResolverRuleAssociation` resources are present in the graph. Details of the resolver rules (forwarding targets, associated VPCs) are not included in the provided discovery data.

---

## Assumptions

| Assumption | Basis | How to Validate |
|---|---|---|
| Subnets without IGW routes (`subnet-0980c559fb5dcb640`, `subnet-06300b25ee5d11e54` in `vpc-06e0352782cb28256`; `subnet-0be5d8818bcffe1c6`, `subnet-0da2ee949655c0819`, `subnet-0f56715e273de2bf9` in `vpc-0d7e76c09908626c2`) are private | No IGW route detected in their route tables; no NAT gateway exists in either VPC | Confirm with the customer; review full route table entries |
| The `allows_from` relationship between `sg-0929c485a88a87f87` and `sg-0ad00d699b43544b3` represents a backend tier receiving traffic from the ALB | Relationship type in graph data | Review the actual ingress rule on `sg-0929c485a88a87f87` |

---

## Unknowns / Gaps

- Names, AZs, and CIDR blocks for `subnet-0980c559fb5dcb640`, `subnet-06300b25ee5d11e54`, `subnet-0be5d8818bcffe1c6`, `subnet-0da2ee949655c0819`, and `subnet-0f56715e273de2bf9` are not present in the provided data.
- The resource type and purpose of the ENI `eni-0ca3748ec1f2a155b` (which holds the Elastic IP `eipalloc-0d1376aefaed86d40`) is not identified. It is in public subnet `subnet-013918fce0a052014` and secured by the ALB security group, which warrants clarification.
- Ingress and egress rules for `sg-0929c485a88a87f87`, `sg-054012adde14f592e`, `sg-05dcc17603d8eee7a`, `sg-0bbc1f0c3f75e4086`, and `sg-05a6a6b5e78bd9da4` are not present in the provided data.
- The two NACLs beyond `acl-0c70bab77515a89f5` are not described in the provided data.
- Route 53 Resolver rule targets and VPC associations are not present in the provided data.
- No environment tag was detected on `vpc-0d7e76c09908626c2` (`test-vpc`). Its purpose and ownership should be confirmed.
- Whether the ALB listener on port 80 redirects to HTTPS or serves traffic directly is not determinable from the provided data.

---

## Follow-Up Questions

1. What resource holds the Elastic IP `eipalloc-0d1376aefaed86d40` on ENI `eni-0ca3748ec1f2a155b` in the public subnet — is this an EC2 instance or another service, and is direct internet access to it intentional?
2. The ALB (`dx-discovery-test-alb`) exposes port 80 to `0