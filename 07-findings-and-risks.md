# Findings & Risks — aws-account-118535197059

> **Public Example Report — Sanitized**
> This is a sanitized demonstration report generated by [CloudoX](https://cloudox.io).
> AWS account IDs, resource identifiers (VPC, subnet, security-group, IAM role, etc.),
> organisation IDs, and any customer- or employer-identifying names have been replaced
> with realistic synthetic values using a deterministic mapping.
> Architecture patterns, relationships, and findings are preserved for demonstration
> purposes. This report does **not** represent any real AWS environment or customer.

---

Seven findings were generated across two categories: **exposure** (internet-facing resources) and **security group permissiveness**. No explicit risk records were generated (`risk_count: 0`). The environment is classified as **sandbox** (high confidence, explicit tag), which is relevant context when assessing follow-up priority.

---

## Finding Summary

| # | Title | Category | Confidence | Impacted Resource(s) | Follow-up Priority |
|---|-------|----------|------------|----------------------|--------------------|
| F1 | ALB exposed to the public internet | exposure | high | `dx-discovery-test-alb` | High — internet-facing load balancer in a sandbox; validate whether public exposure is intentional |
| F2–F6 | Five subnets exposed to the public internet (IGW route) | exposure | high | See table below | Medium — public subnets are expected if the ALB is intentionally internet-facing, but scope should be confirmed |
| F7 | Security group `sg-0ad00d699b43544b3` allows ingress from `0.0.0.0/0` or `::/0` | exposure | high | `sg-0ad00d699b43544b3` (`dx-discovery-test-sg-alb`) | High — unrestricted ingress on an ALB-attached SG warrants explicit review of allowed ports |

> **Follow-up priority basis:** No explicit severity field is present on any finding. Priorities above are based on the combination of (a) internet-facing scheme on the ALB, (b) unrestricted CIDR on the attached security group, and (c) sandbox classification — which reduces production-impact concern but does not eliminate exposure risk if the sandbox shares credentials, data, or network paths with other environments.

---

## F1 — Internet-Facing Load Balancer

**Finding:** `exposure:AWS::ElasticLoadBalancingV2::LoadBalancer:dx-discovery-test-alb`

The load balancer `dx-discovery-test-alb` has `scheme: internet-facing` (high-confidence finding). It is the anchor resource for the inferred workload `dx-discovery-test-alb` (account `118535197059`, region `eu-central-1`).

**Impact context:** An internet-facing ALB in a sandbox account is reachable from the public internet. Combined with finding F7 (unrestricted ingress SG), the effective exposure depends entirely on the listener rules and target group configuration. The workload confidence is **medium**, so the full resource scope should be validated with the customer.

**Suggested follow-up:** Confirm with the account owner whether public internet access to `dx-discovery-test-alb` is intentional. If not, change the scheme to `internal`. If intentional, verify that the listener (`AWS::ElasticLoadBalancingV2::Listener` — one detected) restricts traffic to expected ports and paths, and that the target group `dx-discovery-test-tg` does not expose unintended backends.

---

## F2–F6 — Public Subnets (IGW Route)

Five subnets have a route table entry forwarding traffic to an Internet Gateway (high-confidence finding on each):

| Finding ID | Subnet |
|------------|--------|
| F2 | `subnet-013918fce0a052014` |
| F3 | `subnet-02cfd56481b2491e5` |
| F4 | `subnet-0a0bd62eb89349282` |
| F5 | `subnet-07f04a1f0266b49d2` |
| F6 | `subnet-0d65afe24e85aacbe` |

Two VPCs and two Internet Gateways are present in the graph (`AWS::EC2::VPC: 2`, `AWS::EC2::InternetGateway: 2`). No relationship data was retained for this section, so subnet-to-VPC assignments cannot be confirmed from the available data.

**Impact context:** Public subnets are a prerequisite for an internet-facing ALB, so findings F2–F6 are consistent with F1. The concern is whether any EC2 instances (`AWS::EC2::Instance: 2` detected) or other resources are placed directly in these public subnets with public IP assignment enabled — that is not determinable from the current data.

**Suggested follow-up:** For each of the five subnets, confirm: (a) which VPC it belongs to, (b) whether `MapPublicIpOnLaunch` is enabled, and (c) which resource types are currently placed in it. Pay particular attention to the two EC2 instances detected in the graph.

---

## F7 — Unrestricted Ingress on ALB Security Group

**Finding:** `exposure:AWS::EC2::SecurityGroup:sg-0ad00d699b43544b3`

Security group `sg-0ad00d699b43544b3` (named `dx-discovery-test-sg-alb`) has at least one ingress rule permitting `0.0.0.0/0` or `::/0`. This SG name suggests attachment to the ALB identified in F1, making it the outermost network control for inbound internet traffic.

**Impact context:** The specific ports and protocols covered by the unrestricted rule are not present in the discovery data. If port 443 (HTTPS) is the only open port and a valid ACM certificate is attached (`AWS::CertificateManager::Certificate: 1` detected), the exposure may be intentional. If additional ports are open (e.g., 80 without redirect, or management ports), the risk increases materially.

**Suggested follow-up:** Review the ingress rules on `sg-0ad00d699b43544b3` and enumerate every port/protocol combination covered by the `0.0.0.0/0` / `::/0` rules. Restrict to the minimum required ports. Validate whether the ACM certificate detected in the account is attached to the ALB listener.

---

## No Explicit Risk Records

`risk_count: 0` — no entries exist in the `risks` collection. The seven findings above are the only machine-generated signals available. Additional risks may exist but were not surfaced by the discovery run (see `08-assumptions-and-unknowns.md` for gaps).

---

## Unknowns Relevant to This Section

| Unknown | Why It Matters | How to Resolve |
|---------|---------------|----------------|
| Ingress port/protocol detail for `sg-0ad00d699b43544b3` | Cannot determine whether unrestricted ingress covers only expected ports (e.g., 443) or broader exposure | Pull `aws ec2 describe-security-groups --group-ids sg-0ad00d699b43544b3` |
| Subnet-to-VPC mapping for the five public subnets | Cannot confirm whether public subnets span one or both VPCs, or which workload they serve | Pull `aws ec2 describe-subnets` filtered to the five subnet IDs |
| EC2 instance placement and public IP assignment | Two instances detected; unknown whether they reside in public subnets with public IPs | Pull `aws ec2 describe-instances` and check `SubnetId` and `PublicIpAddress` fields |
| ALB listener rules and target group health | Workload confidence is medium; listener and target group configuration not available | Pull `aws elbv2 describe-listeners` and `describe-rules` for `dx-discovery-test-alb` |
| GuardDuty detector status | One `AWS::GuardDuty::Detector` resource is present but its enabled/active state is unknown | Validate in the GuardDuty console or via `aws guardduty get-detector` |