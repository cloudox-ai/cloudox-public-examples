# Security Overview — Account `118535197059` (Sandbox)

> **Public Example Report — Sanitized**
> This is a sanitized demonstration report generated by [CloudoX](https://cloudox.io).
> AWS account IDs, resource identifiers (VPC, subnet, security-group, IAM role, etc.),
> organisation IDs, and any customer- or employer-identifying names have been replaced
> with realistic synthetic values using a deterministic mapping.
> Architecture patterns, relationships, and findings are preserved for demonstration
> purposes. This report does **not** represent any real AWS environment or customer.

---

## Internet Exposure

Three high-confidence exposure findings are directly evidenced in the discovery data.

| Resource | Type | Finding | Evidence |
|---|---|---|---|
| `dx-discovery-test-alb` | ALB | Internet-facing scheme | Finding `exposure:AWS::ElasticLoadBalancingV2::LoadBalancer:dx-discovery-test-alb` |
| `sg-0ad00d699b43544b3` (`dx-discovery-test-sg-alb`) | Security Group | Ingress `0.0.0.0/0` on port 80 | Finding `exposure:AWS::EC2::SecurityGroup:sg-0ad00d699b43544b3`; digest `security_groups.world_open[0].world_open_ports: [80]` |
| `subnet-013918fce0a052014`, `subnet-02cfd56481b2491e5`, `subnet-0a0bd62eb89349282`, `subnet-07f04a1f0266b49d2`, `subnet-0d65afe24e85aacbe` | EC2 Subnets | Route table forwards to an Internet Gateway | Findings `exposure:AWS::EC2::Subnet:*` (5 findings) |

**Port 80 is the only world-open port detected.** The digest field `security_groups.world_open[0].sensitive_world_ports` is empty, meaning no ports commonly classified as sensitive (e.g., 22, 3389, 443 with self-signed, database ports) were flagged by the digest. This should be validated against the actual listener configuration on `dx-discovery-test-alb`.

The relationship `sg-0929c485a88a87f87` → `sg-0ad00d699b43544b3` (`allows_from`) indicates a second security group is permitted to send traffic to `sg-0ad00d699b43544b3`. The identity and purpose of `sg-0929c485a88a87f87` should be confirmed with the customer.

Of the 6 total security groups, 5 are classified as internal-only (digest `security_groups.internal_only: 5`).

---

## IAM Shape

| Metric | Value | Source |
|---|---|---|
| Total IAM roles | 67 | digest `iam.total_roles` |
| Customer-defined roles | 41 | digest `iam.customer_roles` |
| Service-linked roles | 26 | digest `iam.service_linked_roles` |
| IAM users | 0 | digest `iam.users` |
| Groups | 1 | digest `iam.groups` |
| Customer-managed policies | 5 | digest `iam.customer_managed_policies` |

**No IAM users are present.** Access appears to be federated via AWS SSO (IAM Identity Center), based on the presence of four `AWSReservedSSO_*` roles:

- `AWSReservedSSO_AWSAdministratorAccess_a57b788e1e0f9efa` (`AROAAAAAAGMSR3YMEM0Y3`)
- `AWSReservedSSO_AWSPowerUserAccess_ea853376d39b797e` (`AROAAAAABDXUATKTYHG6Y`)
- `AWSReservedSSO_AWSOrganizationsFullAccess_79377233e9209da5` (`AROAAAAABJO9YWJU3XUR8`)
- `AWSReservedSSO_AcmeCorpSAM-AcmeCorp_ab05e35c93b73cdc` (`AROAAAAACF4FXC7ERXSB0`)

These are indirect signals only. The SSO configuration, permission set assignments, and active sessions are not visible in the discovery data and should be validated with the customer.

**Admin-like roles (digest `iam.admin_like_role_refs`):**

| Role ARN | Resource ID |
|---|---|
| `aws-controltower-AdministratorExecutionRole` | `AROAAAAAB649RFEHJ7YKJ` |
| `AWSReservedSSO_AWSAdministratorAccess_a57b788e1e0f9efa` | `AROAAAAAAGMSR3YMEM0Y3` |
| `AWSReservedSSO_AWSPowerUserAccess_ea853376d39b797e` | `AROAAAAABDXUATKTYHG6Y` |
| `DevOpsAgentRole-WebappAdmin-fxxh2pbx` | `AROAAAAACS4P50FVU22DC` |

The `DevOpsAgentRole-WebappAdmin-fxxh2pbx` (`AROAAAAACS4P50FVU22DC`) is a service-role (path `service-role/`) flagged as admin-like. Its trust policy, attached policies, and usage context are not visible in the discovery data.

`AWSControlTowerExecution` (`AROAAAAADMJ03XJ1A86BA`) and `aws-controltower-AdministratorExecutionRole` (`AROAAAAAB649RFEHJ7YKJ`) are Control Tower cross-account execution roles. Their presence is consistent with a Control Tower-managed account but does not confirm active Control Tower governance.

The `service_principals.config: 1` entry in the digest indicates one role has a Config service principal in its trust policy. The role `aws-controltower-ConfigRecorderRole` (`AROAAAAACK3VVV2X3NALJ`) is the likely candidate based on its name, but this is an inference and should be validated.

---

## Security Services — Presence and State

| Service | Signal | State |
|---|---|---|
| GuardDuty | 1 detector resource present (`AWS::GuardDuty::Detector`) | **Enabled state unknown** — no status field in discovery data |
| Security Hub | No hub resource detected | Not detected; digest `unknowns` explicitly states: *"No Security Hub enablement discovered."* |
| AWS Config | 0 recorders, 0 rules (digest `monitoring.config_recorders: 0`, `config_rules: 0`) | No recorder or rules detected |
| Access Analyzer | 0 analyzers (digest `monitoring.access_analyzers: 0`) | No analyzer detected |
| CloudTrail | Trail `arn:aws:cloudtrail:us-east-1:154823011438:trail/aws-controltower-BaselineCloudTrail` present | Trail is multi-region and has log file validation enabled; **active state unknown** — no status field |

**Important:** The CloudTrail trail is in account `154823011438`, not in the scoped account `118535197059`. Whether this trail covers account `118535197059` (e.g., as an organization trail) cannot be confirmed from the discovery data alone.

No explicit risk records were generated (digest `risk_count: 0`), but the absence of detected Config recorders, Access Analyzer, and Security Hub are material gaps that should be discussed with the customer.

---

## Encryption and Secrets

| Area | Observation | Source |
|---|---|---|
| KMS keys | 5 total: 4 AWS-managed, 1 customer-managed | digest `kms` |
| KMS aliases | 16 aliases across 5 keys | digest `kms.aliases: 16` |
| Secrets Manager | 1 secret present | digest `secrets.count: 1` |
| ACM certificates | 1 certificate present | digest `certificates.count: 1` |

The single customer-managed KMS key's usage scope (which services or resources it protects) is not visible in the discovery data. The 1 Secrets Manager secret's content, rotation configuration, and consuming resource are also not visible.

---

## S3 Versioning Gaps

The digest identifies 3 S3 buckets without versioning enabled:

| Bucket | Region |
|---|---|
| `cf-templates-aaaaaa8ydqwx-eu-central-1` | eu-central-1 |
| `cf-templates-aaaaab5vef96-eu-north-1` | eu-north-1 |
| `cf-templates-aaaaaahskpta-eu-west-3` | eu-west-3 |

These appear to be CloudFormation template staging buckets based on their naming convention. Their public access block configuration and bucket policies are not visible in the discovery data.

---

## Backup Coverage

No AWS Backup plans or vaults were detected (digest `backups.backup_plans: 0`, `backup_vaults: 0`). Whether the two EC2 instances (`AWS::EC2::Instance` count: 2) or other stateful resources have snapshot-based or alternative backup coverage is not visible in the discovery data.

---

## Unknowns / Gaps

| Gap | Detail |
|---|---|
| GuardDuty detector state | Resource present; enabled/disabled status not in discovery data |
| Security Hub | Explicitly listed as unknown in digest; no hub resource found |
| AWS Config | No recorder or rules detected; whether this is intentional for a sandbox account is unconfirmed |
| Access Analyzer | No analyzer detected; scope of IAM and S3 access analysis is unknown |
| CloudTrail coverage | Trail is in account `154823011438`; whether it covers `118535197059` as an org trail is unconfirmed |
| `sg-0929c485a88a87f87` | Permits traffic into `sg-0ad00d699b43544b3`; identity and purpose not visible |
| `DevOpsAgentRole-WebappAdmin-fxxh2pbx` | Admin-like service role; trust policy and usage not visible |
| SSO permission set assignments | Four SSO-backed roles present; active assignments and MFA enforcement not visible |
| Customer-managed KMS key | Key usage scope not visible |
| Secrets Manager secret | Rotation config and consuming resource not visible |
| S3 bucket policies and public access blocks | Not visible for any of the 7 buckets |

---

## Follow-up Questions

1. Is the GuardDuty detector in account `118535197059` currently enabled, and is it reporting to a delegated administrator account?
2. Is Security Hub intentionally not enabled in this sandbox account, or is it expected to be managed centrally?
3. Is the CloudTrail trail `aws-controltower-BaselineCloudTrail` (account `154823011438`) an organization trail that covers `118535197059`? If so, where are the logs delivered?
4. Is AWS Config intentionally disabled in this sandbox, or is it expected to be enabled via Control Tower or LZA?
5. What is the purpose and trust policy of `DevOpsAgentRole-WebappAdmin-fxxh2pbx`, and which principal or service assumes it?
6. What does the single Secrets Manager secret contain, and is rotation enabled?
7. Is port 80 on `dx-discovery-test-alb` / `sg-0ad00d699b43544b3` intentional (e.g., HTTP redirect to HTTPS), and is there a corresponding HTTPS listener?