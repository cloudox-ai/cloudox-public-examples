# Account & Environment Structure

> **Public Example Report — Sanitized**
> This is a sanitized demonstration report generated by [CloudoX](https://cloudox.io).
> AWS account IDs, resource identifiers (VPC, subnet, security-group, IAM role, etc.),
> organisation IDs, and any customer- or employer-identifying names have been replaced
> with realistic synthetic values using a deterministic mapping.
> Architecture patterns, relationships, and findings are preserved for demonstration
> purposes. This report does **not** represent any real AWS environment or customer.

---

## Account

| Field | Value |
|---|---|
| Account ID | `118535197059` |
| Human-readable name | `aws-account-118535197059` |
| Organization membership | No organization detected in the provided discovery data |

The account name `aws-account-118535197059` appears to be a default or auto-generated label rather than a meaningful human name. Whether a more descriptive alias exists in the AWS Console or AWS Organizations should be validated with the customer.

---

## Environment Classification

| Classification | Confidence | Explicit | Evidence |
|---|---|---|---|
| Sandbox | High | Yes (`explicit: true`) | Environment record `118535197059-sandbox` in discovery data |

The environment is classified as **sandbox** with high confidence. The `explicit: true` flag indicates this classification was directly asserted in the discovery data, not inferred from naming conventions or tags alone.

---

## Regions

The `graph_stats` block reports `region_count: 0`, meaning no region entities were independently discovered. However, both detected workloads are anchored to **`eu-central-1`** (Frankfurt):

| Workload | Anchor Resource | Region | Confidence |
|---|---|---|---|
| `dx-discovery-test-alb` | `AWS::ElasticLoadBalancingV2::LoadBalancer` | `eu-central-1` | Medium |
| `dx-discovery-test-discovery` | `AWS::Lambda::Function dx-discovery-test-discovery` | `eu-central-1` | Low |

`eu-central-1` is the only region evidenced by workload anchors. Whether resources exist in other regions is not determinable from the available data. This should not be treated as confirmation that the account is single-region.

---

## Tagging Coverage

No tag values (e.g. `Environment`, `Owner`, `Project`, `CostCenter`) are present in the filtered discovery payload for this section. The following observations apply:

- **Environment tag**: Not observed on individual resources. The sandbox classification derives from the environment record, not from resource-level tags.
- **Owner / Project / CostCenter tags**: No evidence found for any of these tag keys on any resource in the provided data.
- **Workload naming prefix `dx-discovery-test-`**: Present on the ALB (`dx-discovery-test-alb`), target group (`dx-discovery-test-tg`), and Lambda function (`dx-discovery-test-discovery`). This is an observed naming pattern; what `dx` refers to (a team, product, or project) is not stated in the data and should be validated with the customer.

The absence of tag evidence in this payload does not confirm that no tags exist on resources — it means tags were not surfaced in the discovery data provided for this section.

---

## Ownership & Lifecycle Indicators

No explicit ownership, cost-centre, or lifecycle tags were detected. The 277 total resources across the account are associated with a single account and a single sandbox environment record. No system or cost-centre groupings were detected (`system_count: 0`).

---

## Assumptions

| # | Assumption | Basis | How to Validate |
|---|---|---|---|
| 1 | `eu-central-1` is the only active region for this account | Both workload anchors reference `eu-central-1`; no other region appears in the data | Ask the customer; review AWS Console → Account → Regions or run a multi-region resource inventory |
| 2 | The `dx-discovery-test-` prefix denotes a test or discovery-specific workload, not a production system | Sandbox environment classification + naming convention | Confirm with the account owner whether these resources are ephemeral or representative of a real workload pattern |

---

## Unknowns / Gaps

- **Account alias**: The name `aws-account-118535197059` appears auto-generated. The actual AWS account alias (if set) is unknown.
- **Organization context**: `organization_count: 0` — it is unknown whether this account is standalone or a member of an AWS Organization that was not in scope for discovery.
- **Multi-region footprint**: Region entities were not discovered. Resources in regions other than `eu-central-1` cannot be ruled out.
- **Resource-level tags**: No tag data was surfaced in this section's payload. Tagging coverage across the 277 resources cannot be assessed.
- **Ownership mapping**: No Owner, Team, or CostCenter tags were detected. It is unknown who is responsible for individual resources or workloads.

---

## Follow-up Questions

1. Does this account have an AWS account alias set, and is it part of an AWS Organization?
2. Are there resources deployed in regions other than `eu-central-1`?
3. What does the `dx-` prefix represent — a team, product, or project name?
4. Is a mandatory tagging policy (Owner, Project, CostCenter, Environment) in place or planned for this account?
5. Are the `dx-discovery-test-*` resources ephemeral (created for this discovery exercise) or representative of a persistent workload?