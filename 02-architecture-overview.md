# Architecture Overview — aws-account-118535197059

> **Public Example Report — Sanitized**
> This is a sanitized demonstration report generated by [CloudoX](https://cloudox.io).
> AWS account IDs, resource identifiers (VPC, subnet, security-group, IAM role, etc.),
> organisation IDs, and any customer- or employer-identifying names have been replaced
> with realistic synthetic values using a deterministic mapping.
> Architecture patterns, relationships, and findings are preserved for demonstration
> purposes. This report does **not** represent any real AWS environment or customer.

---

**Account:** 118535197059 | **Region:** eu-central-1 | **Total resources:** 277 | **Environment:** sandbox (explicit, high confidence)

---

## Detected Workloads

Two inferred workloads were identified by the digest. Neither carries an explicit `Environment` / `Stage` / `Tier` tag on its anchor resource; classification relies on inference.

| Workload name | Anchor resource | Anchor type | Members detected | Confidence | Internet-facing |
|---|---|---|---|---|---|
| `dx-discovery-test-alb` | `dx-discovery-test-alb` | `AWS::ElasticLoadBalancingV2::LoadBalancer` | 3 | Medium | Yes (finding-confirmed) |
| `dx-discovery-test-discovery` | `dx-discovery-test-discovery` | `AWS::Lambda::Function` | 1 | Low | No |

> **Note:** 260 of 277 resources carry no environment/stage/tier tag (digest `unknowns`). Workload groupings above are inferred by the discovery engine, not declared by the customer.

---

## Application Layer

**Workload: `dx-discovery-test-alb`** (medium confidence)

The digest and relationships confirm a three-resource chain:

- **ALB** `dx-discovery-test-alb` — internet-facing Application Load Balancer (finding `exposure:AWS::ElasticLoadBalancingV2::LoadBalancer:dx-discovery-test-alb`; digest `entry_points`).
- **Listener** `13f305d777270397` — attached to the ALB (`part_of` relationship) and routes traffic to the target group (`routes_to` dependency).
- **Target group** `dx-discovery-test-tg` — registered as `member_of` the ALB and resident in `vpc-06e0352782cb28256`.

No EC2 instances or Lambda functions are detected as registered targets of `dx-discovery-test-tg` in the provided relationship data. Whether the target group has active, healthy targets is unknown.

**Workload: `dx-discovery-test-discovery`** (low confidence)

A single Lambda function `dx-discovery-test-discovery` (eu-central-1) uses IAM role `AROAAAAACN7XY2NNE5ZS2` (`uses_iam_role` dependency). No triggers, event sources, VPC attachment, or downstream dependencies are present in the relationship data. The function's purpose cannot be determined from the available data.

**Remaining compute:** The digest records 2 `AWS::EC2::Instance` resources and 1 `AWS::Lambda::Function` (the discovery function above). The two EC2 instances are not associated with either detected workload in the relationship data; their state (running / stopped / terminated) and purpose are unknown from the provided context.

---

## Network Layer

Two VPCs are present in the account:

| VPC | Associated resources (from relationships) |
|---|---|
| `vpc-06e0352782cb28256` | ALB `dx-discovery-test-alb`, target group `dx-discovery-test-tg`, security groups `sg-0ad00d699b43544b3`, `sg-0929c485a88a87f87`, `sg-05dcc17603d8eee7a`, `sg-054012adde14f592e`; subnets `subnet-07f04a1f0266b49d2`, `subnet-013918fce0a052014`, `subnet-0980c559fb5dcb640`, `subnet-06300b25ee5d11e54` |
| `vpc-0d7e76c09908626c2` | Security groups `sg-05a6a6b5e78bd9da4`, `sg-0bbc1f0c3f75e4086`; subnets `subnet-0a0bd62eb89349282`, `subnet-0d65afe24e85aacbe`, `subnet-02cfd56481b2491e5`, `subnet-0be5d8818bcffe1c6`, `subnet-0da2ee949655c0819`, `subnet-0f56715e273de2bf9` |

**Internet exposure (observed):**
- The ALB `dx-discovery-test-alb` is placed in public subnets `subnet-07f04a1f0266b49d2` and `subnet-013918fce0a052014` (both in `vpc-06e0352782cb28256`), both confirmed internet-facing via route-table → IGW `igw-09fc956d52f614cb0` (dependency `rtb-0b8e0917e18420ccf → igw-09fc956d52f614cb0`).
- Security group `sg-0ad00d699b43544b3` (`dx-discovery-test-sg-alb`) has ingress rules open to `0.0.0.0/0` or `::/0` (finding `exposure:AWS::EC2::SecurityGroup:sg-0ad00d699b43544b3`) and secures the ALB (`secured_by` relationship).
- Security group `sg-0929c485a88a87f87` allows traffic from `sg-0ad00d699b43544b3` (`allows_from` relationship), suggesting [**inferred**] it may be applied to backend resources that accept traffic from the ALB SG. This should be validated.
- Five subnets across both VPCs are confirmed public (route-table → IGW findings). Three (`subnet-0a0bd62eb89349282`, `subnet-0d65afe24e85aacbe`, `subnet-02cfd56481b2491e5`) are in `vpc-0d7e76c09908626c2`; two (`subnet-07f04a1f0266b49d2`, `subnet-013918fce0a052014`) are in `vpc-06e0352782cb28256`.

**VPC Endpoints (observed):**
Three route tables route to VPC endpoints: `vpce-050c321468753e823` (referenced by `rtb-0058fbf3c6b8f8d4f`, `rtb-0d47dbf74603a4d27`, `rtb-092aebf7bd0a767c3`) and `vpce-0e58252535f32f873` (referenced by `rtb-0550412284ed21dab`). The endpoint service types are not present in the provided data.

**Internet Gateways:** Two IGWs are present (`igw-09fc956d52f614cb0`, `igw-0fc7848b4f015a3cc`), one per VPC based on route-table dependencies. This is consistent with both VPCs having public subnets, but the second IGW's VPC association is not confirmed in the relationship data.

---

## Data Layer

Seven S3 buckets are recorded in the digest (`storage.by_type`). No bucket names, policies, or associations to the detected workloads are present in the filtered relationship data. Their purpose and ownership are unknown.

One `AWS::SecretsManager::Secret` is present in the account. No relationship to a workload or compute resource was detected.

Five KMS keys and 16 KMS aliases are present. No key-to-resource relationships are present in the filtered data.

---

## Integration Layer

- **EventBridge:** One `AWS::Events::Rule` is present. No target or source relationship was detected in the provided data.
- **Lambda IAM:** `dx-discovery-test-discovery` uses role `AROAAAAACN7XY2NNE5ZS2` (`uses_iam_role` dependency). The role's permissions are not visible in the filtered data.
- No API Gateway, SQS, SNS, or other integration resources were detected in the provided discovery data.

---

## Supporting / Unassociated Resources

The following resource types are present in the account but are not connected to either detected workload in the relationship data:

| Resource type | Count | Notes |
|---|---|---|
| `AWS::EC2::Instance` | 2 | State and purpose unknown; not linked to any workload |
| `AWS::CloudFormation::Stack` | 3 | May be the provisioning mechanism for observed resources — **inferred** from co-presence; not confirmed by relationships |
| `AWS::IAM::Role` | 67 | Majority unassociated in filtered data |
| `AWS::EC2::ManagedPrefixList` | 14 | No relationships detected |
| `AWS::CertificateManager::Certificate` | 1 | Not linked to the ALB in the provided relationship data |
| `AWS::Route53Resolver::ResolverRule` | 1 | Associated with 2 VPCs via `ResolverRuleAssociation`; details not in filtered data |

---

## Inferred Architecture — Confidence Table

| Inference | Basis | Confidence | Validation needed |
|---|---|---|---|
| ALB fronts a backend service via `dx-discovery-test-tg` | Listener `13f305d777270397 → routes_to → dx-discovery-test-tg` (dependency) | Medium | Confirm registered targets and their type |
| `sg-0929c485a88a87f87` is applied to ALB backend resources | `allows_from sg-0ad00d699b43544b3` relationship | Low | Identify which instances/ENIs carry this SG |
| Two VPCs serve distinct purposes | Naming (`test-subnet-public*` vs `dx-discovery-test-public-*`) and separate IGWs | Low | Confirm VPC roles with the customer |
| CloudFormation stacks provisioned the observed resources | 3 stacks present; naming prefix `dx-discovery-test` is consistent | Low | Review stack resources in console |
| `dx-discovery-test-discovery` Lambda is an operational/tooling function | Name contains "discovery"; single-member workload with no detected triggers | Low | Confirm function purpose and invocation pattern |

---

## Internet-Facing Entry Points

| Resource | ID | Type | Confirmation basis |
|---|---|---|---|
| Application Load Balancer | `dx-discovery-test-alb` | `AWS::ElasticLoadBalancingV2::LoadBalancer` | Digest `entry_points`; finding `exposure:AWS::ElasticLoadBalancingV2::LoadBalancer:dx-discovery-test-alb` |

No other internet-facing entry points were confirmed by the digest or findings.

---

## Unknowns / Gaps

- **Target group membership:** No registered targets for `dx-discovery-test-tg` are present in the relationship data. It is unknown whether the ALB is currently serving live traffic.
- **EC2 instance state and role:** The two EC2 instances are not linked to any workload, subnet, or security group in the filtered data. Their running state is unknown.
- **S3 bucket identity and purpose:** Seven buckets are recorded but no names or workload associations are available in the filtered data.
- **VPC endpoint service types:** `vpce-050c321468753e823` and `vpce-0e58252535f32f873` are referenced in route tables but their service endpoints are not in the provided data.
- **ACM certificate binding:** One certificate is present but no `secured_by` or `part_of` relationship to the ALB listener was detected.
- **Lambda triggers:** No event source or trigger relationship was detected for `dx-discovery-test-discovery`.

---

## Follow-up Questions

1. What backend targets (EC2, ECS, Lambda) are registered in `dx-discovery-test-tg`, and are they currently receiving traffic?
2. What are the two EC2 instances (`AWS::EC2::Instance` × 2), and are they running or stopped?
3. What is the purpose of the second VPC (`vpc-0d7e76c09908626c2`) — is it a shared-services, legacy, or separate application VPC?
4. What services do `vpce-050c321468753e823` and `vpce-0e58252535f32f873` terminate (e.g. S3, SSM, ECR)?
5. Is the ACM certificate bound to the ALB listener, and is HTTPS termination configured on `13f305d777270397`?
6. What invokes `dx-discovery-test-discovery`, and does it have access to production data or cross-account resources?