# Observability – Logging, Monitoring & Visibility Signals

> **Public Example Report — Sanitized**
> This is a sanitized demonstration report generated by [CloudoX](https://cloudox.io).
> AWS account IDs, resource identifiers (VPC, subnet, security-group, IAM role, etc.),
> organisation IDs, and any customer- or employer-identifying names have been replaced
> with realistic synthetic values using a deterministic mapping.
> Architecture patterns, relationships, and findings are preserved for demonstration
> purposes. This report does **not** represent any real AWS environment or customer.

---

Account `118535197059` (sandbox) · Region `eu-central-1` (primary observed) · `us-east-1` (trail)

---

## CloudTrail

A single CloudTrail trail resource is present in the graph:

| Field | Value |
|---|---|
| Trail name | `aws-controltower-BaselineCloudTrail` |
| ARN | `arn:aws:cloudtrail:us-east-1:154823011438:trail/aws-controltower-BaselineCloudTrail` |
| Home region | `us-east-1` |
| Multi-region | `true` |
| Organization trail | `true` |
| Global service events | `true` |
| Log file validation | `true` |
| S3 bucket | `aws-controltower-logs-140101216008-us-east-1` |
| S3 key prefix | `o-aaabv78by9` |
| CloudWatch Logs log group | `arn:aws:logs:us-east-1:154823011438:log-group:aws-controltower/CloudTrailLogs:*` |
| SNS topic | `arn:aws:sns:us-east-1:133092875746:aws-controltower-AllConfigNotifications` |

**Observed:** The trail resource exists with `is_multi_region_trail=true`, `is_organization_trail=true`, and `log_file_validation_enabled=true`. The trail was discovered via account `118535197059` but is owned by account `154823011438`, which appears to be a Control Tower management or log-archive account. No `status` or `is_logging` field is present in the discovery data; whether the trail is actively recording cannot be confirmed from the available data alone.

**Note:** The CloudWatch Logs log group referenced by the trail (`aws-controltower/CloudTrailLogs`) is in account `154823011438` / `us-east-1` and is not among the four log groups discovered in account `118535197059`. Its retention, metric filters, and stored bytes are unknown.

---

## CloudWatch Log Groups

Four log groups were discovered in account `118535197059` / `eu-central-1`:

| Log Group | Retention (days) | Metric Filters | Stored Bytes | Notes |
|---|---|---|---|---|
| `/aws/clientvpn/sandbox-client-vpn` | 14 | 0 | 0 | No metric filters; zero stored bytes |
| `/aws/lambda/dx-discovery-test-discovery` | 7 | 0 | 0 | No metric filters; zero stored bytes |
| `/aws/vendedlogs/events/event-bus/test-event-bus` | **Not set** | 0 | 0 | No retention policy; zero stored bytes |
| `/cloudox/dx-discovery-test/app` | 7 | 0 | 0 | No metric filters; zero stored bytes |

**Observations:**

- `/aws/vendedlogs/events/event-bus/test-event-bus` has no `retention_in_days` value in the discovery data. Without a retention policy, logs in this group would accumulate indefinitely (and incur unbounded storage cost). This should be validated with the customer.
- All four log groups report `stored_bytes=0`. This may indicate the groups were recently created, that the associated services have not yet produced log output, or that log delivery is not yet active. The Lambda log group (`/aws/lambda/dx-discovery-test-discovery`) and the app log group (`/cloudox/dx-discovery-test/app`) share the same creation timestamp cluster, which is consistent with a recent deployment, but the zero-byte state should be confirmed.
- No log group carries any metric filters (`metric_filter_count=0` on all four). There are no CloudWatch metric alarms derived from log-based filters in the discovered data.

---

## CloudWatch Alarms

One alarm was discovered:

| Alarm | Namespace | Metric | Statistic | Threshold | Operator | State |
|---|---|---|---|---|---|---|
| `dx-discovery-test-lambda-errors` | `AWS/Lambda` | `Errors` | `Sum` | `1.0` | `≥` | `OK` |

- **Observed:** The alarm is in state `OK` as of `2026-05-06T17:00:18.592000+02:00`. It monitors Lambda `Errors` over one evaluation period.
- No alarm action targets (SNS topics, Auto Scaling actions, etc.) are present in the discovery data. Whether this alarm notifies anyone on breach cannot be confirmed.
- This is the only alarm in the graph. No alarms exist for ALB, EC2, VPN, or any other service visible in the environment.

---

## EventBridge

One EventBridge rule was discovered:

| Rule | Bus | Schedule | State |
|---|---|---|---|
| `dx-discovery-test-daily-lambda` | `default` | `rate(1 day)` | **DISABLED** |

- **Observed:** The rule state is explicitly `DISABLED`. Its description states: *"Scheduled invocation of discovery-test Lambda (DISABLED unless EnableEventBridgeSchedule=true)."* The rule is not currently triggering the Lambda function.
- No other EventBridge rules are present in the discovery data. The log group `/aws/vendedlogs/events/event-bus/test-event-bus` suggests a custom event bus named `test-event-bus` exists, but no rules targeting that bus were discovered.

---

## GuardDuty

One `AWS::GuardDuty::Detector` resource is present in the graph (count: 1 per `graph_stats`). No detector resource detail (detector ID, `status`, `finding_publishing_frequency`, or enabled data sources) is included in the filtered resource list for this section. The detector resource exists; its activation state is unknown from the available data.

---

## Monitoring Coverage Gaps

The following observability areas have no detected resources:

| Area | Observation |
|---|---|
| CloudWatch Dashboards | Not detected in the provided discovery data |
| Log-based metric filters | Zero metric filters across all four discovered log groups |
| ALB access logging | No log group or S3 bucket association detected for `dx-discovery-test-alb` |
| VPC Flow Logs | No flow log resources detected in the provided discovery data |
| Config recorder / delivery channel | Not detected in the provided discovery data |
| Security Hub | Not detected in the provided discovery data |
| SNS alarm actions | No action ARNs present on `dx-discovery-test-lambda-errors` |
| X-Ray / distributed tracing | Not detected in the provided discovery data |

---

## Assumptions

1. **Trail is owned by a Control Tower management account.** The trail ARN is in account `154823011438`, which differs from the sandbox account `118535197059`. The trail was discovered via the sandbox account. This is consistent with a Control Tower organisation trail pattern, but the account role (management vs. log-archive vs. audit) should be validated with the customer.
   - *Basis:* `discovered_via_account_id=118535197059`, trail `account_id=154823011438`, `is_organization_trail=true`.
   - *Validate:* Confirm account `154823011438` identity and whether the sandbox account's API calls are covered by this trail.

2. **Zero stored bytes reflects recent creation, not absent log delivery.** The Lambda and app log groups share a creation timestamp cluster. It is possible log delivery has not yet occurred rather than being misconfigured.
   - *Basis:* `stored_bytes=0` on all four log groups; `creation_time` values are recent.
   - *Validate:* Check CloudWatch Logs console for any log streams and confirm Lambda has been invoked at least once.

---

## Unknowns / Gaps

- **Trail logging status:** No `is_logging` or equivalent status field is present. Cannot confirm the Control Tower trail is actively recording.
- **CloudTrail log group retention and filters:** The log group `aws-controltower/CloudTrailLogs` (account `154823011438`) is outside the discovered scope. Retention period and any metric-filter-based alerting are unknown.
- **GuardDuty detector status:** Detector resource exists but no `status` field was returned. Active threat detection cannot be confirmed.
- **ALB access logs:** No evidence of access log configuration on `dx-discovery-test-alb`. HTTP request-level visibility is unconfirmed.
- **VPC Flow Logs:** No flow log resources detected. Network-level traffic visibility for `vpc-...` resources is unconfirmed.
- **EventBridge `test-event-bus`:** A vended log group for this bus exists but no rules targeting it were discovered. Its purpose and whether it receives events is unknown.
- **Alarm notification targets:** The single alarm (`dx-discovery-test-lambda-errors`) has no action ARNs in the data. Whether an on-call or alerting path exists is unknown.
- **Retention on `/aws/vendedlogs/events/event-bus/test-event-bus`:** No `retention_in_days` value present. Confirm whether a retention policy has been applied outside of what was captured.

---

## Follow-up Questions

1. Is account `154823011438` the Control Tower log-archive or audit account? Does the organisation trail cover API activity in account `118535197059`?
2. Has the Lambda function (`dx-discovery-test-discovery`) been invoked since deployment? If so, why does `/aws/lambda/dx-discovery-test-discovery` show zero stored bytes?
3. Are VPC Flow Logs intentionally disabled across all VPCs in this account, or is this an oversight?
4. Is ALB access logging required for this workload? If so, which S3 bucket or log group should receive it?
5. What is the intended trigger for enabling the EventBridge rule `dx-discovery-test-daily-lambda`? Is `EnableEventBridgeSchedule=true` a CloudFormation parameter that needs to be set?
6. Should the alarm `dx-discovery-test-lambda-errors` have an SNS action configured to notify an operator on breach?
7. What is the purpose of the custom event bus `test-event-bus`, and are events expected to flow through it?